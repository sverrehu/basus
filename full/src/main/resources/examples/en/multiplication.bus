/*
    Shows the multiplication table for the number 7.
    Try changing the mul variable to get tables for
    other numbers.
*/

mul = 7;

for n = 0 to 10 do

    product = mul * n;
    println(mul, " * ", n, " = ", product);

done;
